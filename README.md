# Python Intern Task

This script fetches user data from a public API and prints Name, Username, Email, and City.
The script also prints only users whose city starts with 'S'.

## How to Run

1. Install the requests library:
   pip install requests

2. Run the script:
   python Task1.py
